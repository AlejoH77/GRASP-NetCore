using System;

namespace GraspExamples.Controller.Right
{
    public class PhotoRepository
    {
        internal void UpdatePhoto(Photo photo)
        {
            //Process to update photo
        }

        internal Photo ReadPhoto()
        {
            //Process to read photo
            return new Photo();
        }
    }
}